	<!DOCTYPE html>
<html>
<head>
<style>
div {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #f5be00;
}
            
.btn-donate {
  --clr-font-main: hsla(0 0% 20% / 100);
  --btn-bg-1: hsla(194 100% 69% / 1);
  --btn-bg-2: hsla(217 100% 56% / 1);
  --btn-bg-color: hsla(360 100% 100% / 1);
  --radii: 0.5em;
  cursor: pointer;
  padding: 0.9em 6em;
  min-width: 120px;
  min-height: 44px;
  font-size: var(--size, 1rem);
  font-family: "Segoe UI", system-ui, sans-serif;
  font-weight: 500;
  transition: 0.8s;
  background-size: 280% auto;
  background-image: linear-gradient(325deg, var(--btn-bg-2) 0%, var(--btn-bg-1) 55%, var(--btn-bg-2) 90%);
  border: none;
  border-radius: var(--radii);
  color: var(--btn-bg-color);
  box-shadow: 0px 0px 20px rgba(71, 184, 255, 0.5), 0px 5px 5px -1px rgba(58, 125, 233, 0.25), inset 4px 4px 8px rgba(175, 230, 255, 0.5), inset -4px -4px 8px rgba(19, 95, 216, 0.35);
}

.btn-donate:hover {
  background-position: center right;
}

.btn-donate:is(:focus, :focus-visible, :active) {

  outline: none;
  box-shadow: 0 0 0 3px var(--btn-bg-color), 0 0 0 6px var(--btn-bg-2);
}

@media (prefers-reduced-motion: reduce) {
  .btn-donate {
    transition: linear;
  }
}

.input {
  border-radius: 10px;
  outline: 2px solid #FEBF00;
  border: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
  background-color: #e2e2e2;
  outline-offset: 3px;
  padding: 25px 2.5rem;
  transition: 0.25s;
}
::placeholder{
font-size: 20px
}

.input:focus {
  outline-offset: 5px;
  background-color: #fff
}
div {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%)
  
  
 
  
  
</style>
<meta charset="utf-8" />
<title>"nouveau salon"</title>
</head>
<body>
<h1 style=" text-align: center; font-family: serif">Entrez le nom du salon que vous shouaitez créer</h1>
<form action="/nouveau_salon" method="POST">
<div><input type="text" placeholder="Nom du salon" id="nom_salon" name="nom_salon" class="input" required>
<br>
<button class="btn-donate" type="submit" id="envoyer">
    Envoyer
</button></div>

</body>
</html>